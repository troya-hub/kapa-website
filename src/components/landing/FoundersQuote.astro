---
import KaImage from "@/assets/images/founders/ka.webp?url";
import PaolaImage from "@/assets/images/founders/paola.webp?url";
import Divider from "@/components/ui/Divider.astro";

const founders = [
	{
		name: "Ka",
		job: "Co-Founder",
		image: KaImage,
	},
	{
		name: "Paola",
		image: PaolaImage,
		job: "Co-Founder",
	},
];
---

<div class="site-container">
	<h2
		class="lg:text-k-h4 text-k-h4-mobile text-neutral-10 mx-auto w-[70%] text-center font-semibold"
	>
		Your business graphic design needs covered,
		<span class="text-neutral-60">
			whether it’s marketing assets, presentations, reports, illustrations, gifs, or workbooks.
		</span>
	</h2>

	<Divider />

	<div class="mx-auto flex justify-center gap-x-20">
		{
			founders.map((founder) => (
				<div class="flex gap-x-4">
					<img class="w-16" src={founder.image} alt={founder.name} />
					<div class="flex flex-col justify-center gap-x-4">
						<span class="text-k-lg text-neutral-10 font-semibold">{founder.name}</span>
						<span class="text-k-sm text-neutral-60">{founder.job}</span>
					</div>
				</div>
			))
		}
	</div>
</div>
